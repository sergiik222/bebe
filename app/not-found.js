export default function NotFound({ error }) {
  return (
    <main className="not-found">
      <h1>Not found</h1>
      <p>Unfoirtunately we could not find the requested page</p>
    </main>
  );
}
